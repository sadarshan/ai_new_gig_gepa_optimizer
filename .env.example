# Portkey Configuration
# Required: Your Portkey API key
PORTKEY_API_KEY=pk-your-portkey-api-key-here

# Option 1: Use Portkey Config ID (recommended)
# Create a config in Portkey dashboard and use its ID
PORTKEY_CONFIG_ID=your-config-id-here

# Option 2: Use Virtual Key (if you have one)
# Alternative to config ID for specific provider setups
# PORTKEY_VIRTUAL_KEY=your-virtual-key-here

# Option 3: Direct provider configuration (if not using config ID or virtual key)
# These are already set as defaults in config.py but can be overridden
# PORTKEY_PROVIDER=openai
# PORTKEY_MODEL=gpt-4

# GEPA Optimization Settings (optional - defaults are set in config.py)
# NUM_ITERATIONS=10
# BATCH_SIZE=8
# OUTPUT_DIR=optimization_results